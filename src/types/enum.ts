enum TaskPriority {
  Low = 'Low',
  Medium = 'Medium',
  High = 'High',
}

enum TaskStatus {
  Backlog = 'Backlog',
  InProgress = 'In Progress',
  Done = 'Done',
}

export { TaskPriority, TaskStatus };
